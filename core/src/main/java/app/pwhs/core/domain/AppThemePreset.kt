package app.pwhs.core.domain

enum class AppThemePreset(val label: String) {
    // Fork-added and the default: the 白い熊 black/yellow scheme (see ForkUiDefaults).
    Yellow("Yellow"),
    Orange("Orange"),
    Blue("Blue"),
    Green("Green"),
    Red("Red"),
    Purple("Purple"),
}
